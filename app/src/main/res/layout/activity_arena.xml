<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/frameLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc"
    tools:context=".ArenaActivity">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->

    <ImageView
        android:id="@+id/imageView3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="fitXY"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/battlebackground2x3" />

    <View
        android:id="@+id/view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/playerView"
        android:layout_width="60dp"
        android:layout_height="75dp"
        android:layout_marginLeft="25dp"
        android:layout_marginStart="25dp"
        android:layout_marginTop="120dp"
        android:visibility="visible"
        app:layout_constraintStart_toStartOf="@+id/view"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/adam" />

    <ImageView
        android:id="@+id/enemyView"
        android:layout_width="60dp"
        android:layout_height="75dp"
        android:layout_marginEnd="25dp"
        android:layout_marginRight="25dp"
        android:layout_marginTop="120dp"
        android:scaleX="-1"
        app:layout_constraintEnd_toEndOf="@+id/view"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/adam" />

    <ImageView
        android:id="@+id/playerhpView"
        android:layout_width="42dp"
        android:layout_height="38dp"
        app:layout_constraintBottom_toTopOf="@+id/playerView"
        app:layout_constraintEnd_toEndOf="@+id/playerView"
        app:layout_constraintStart_toStartOf="@+id/playerView"
        app:srcCompat="@drawable/hp" />

    <ImageView
        android:id="@+id/enemyhpView"
        android:layout_width="42dp"
        android:layout_height="38dp"
        app:layout_constraintBottom_toTopOf="@+id/enemyView"
        app:layout_constraintEnd_toEndOf="@+id/enemyView"
        app:layout_constraintStart_toStartOf="@+id/enemyView"
        app:srcCompat="@drawable/hp" />

    <TextView
        android:id="@+id/playerhptextView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginStart="8dp"
        android:gravity="center"
        android:text="100"
        android:textSize="12dp"
        app:layout_constraintBottom_toBottomOf="@+id/playerhpView"
        app:layout_constraintEnd_toEndOf="@+id/playerhpView"
        app:layout_constraintStart_toStartOf="@+id/playerhpView"
        app:layout_constraintTop_toTopOf="@+id/playerhpView" />

    <TextView
        android:id="@+id/enemyhptextView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginStart="8dp"
        android:gravity="center"
        android:text="100"
        android:textSize="12dp"
        app:layout_constraintBottom_toBottomOf="@+id/enemyhpView"
        app:layout_constraintEnd_toEndOf="@+id/enemyhpView"
        app:layout_constraintStart_toStartOf="@+id/enemyhpView"
        app:layout_constraintTop_toTopOf="@+id/enemyhpView" />

    <SurfaceView
        android:id="@+id/surfaceView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="@+id/view"
        app:layout_constraintEnd_toEndOf="@+id/view"
        app:layout_constraintStart_toStartOf="@+id/view"
        app:layout_constraintTop_toTopOf="@+id/view" />

</android.support.constraint.ConstraintLayout>